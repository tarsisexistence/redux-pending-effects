declare namespace Global {
  type PatentDataShape = {
    id?: string,
    title: string,
    description: string,
    imageUrl: string
  }

  type LibraryContentDataShape = {
    id: string,
    title: string,
    link: string,
  }
}